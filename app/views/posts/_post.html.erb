<div class="col-lg-3 col-md-4 col-xs-12 thumb">
	<a class="thumbnail" href="<%= post_path(post.id) %>">
		<%= image_tag post.photo.url, 
		:class => "img-responsive" %>
	</a>	
	<p><%= post.title %></p>
	<p><%= post.location %></p>
</div>
